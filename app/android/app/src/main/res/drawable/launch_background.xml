<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch background: plain themed color that matches the app
     background so the transition into the Flutter animated splash is
     seamless. Adaptive via values/colors.xml (light) and
     values-night/colors.xml (dark). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>